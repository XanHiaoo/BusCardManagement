{
  "name": "electron1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . CardClient --win --out/myClient --arch=x64 --app-version=0.0.1 --electron-version=13.1.1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^13.1.1",
    "electron-packager": "^15.2.0"
  },
  "dependencies": {
    "mssql": "^7.1.0"
  }
}
